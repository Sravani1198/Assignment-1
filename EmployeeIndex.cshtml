@using WebApplication6.Models
@model Employee
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div> 
        @using (Html.BeginForm("My Form"))
        {
        <label> Full Name: @Html.TextBoxFor(x => x.Name)</label>
            <br/>
            <br/>
            <label> Enter Address: @Html.TextBoxFor(x => x.Address)</label>
            <br/>
            <br/>
            <label> Valid User: @Html.CheckBoxFor(x => x.IsEmployee)</label>

        <input type="submit" value="click me" text="Your inputs are submitted"/>
        }
    </div>
</body>
</html>
